{
  "name": "jammin",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspace": [
    "cli"
  ],
  "scripts": {
    "qa": "biome ci",
    "qa-fix": "npm run format; npm run lint",
    "format": "biome format --write",
    "lint": "biome check --write && biome lint --write"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "@types/bun": "^1.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
